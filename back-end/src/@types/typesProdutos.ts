export type Produto = {
    id: string;
    nome: string;
    descricao: string;
    preco: number;
  };

  export type NovoProduto = {
    nome: string;
    descricao: string;
    preco: number;
  };